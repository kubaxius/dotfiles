% flatpak, list, applications
# Print running flatpak applications
flatpak ps --columns=application,pid,child-pid

% git, branch, code
# Change branch
git checkout <branch>
$ branch: git branch | awk '{print $NF}'
