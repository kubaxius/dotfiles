---
- name: Bootstrap local workstation
  hosts: localhost

  roles:
    - role: base
    - role: shell
    - role: hyprland
      hyprland_workstation_features: "{{ workstation_features | default([]) }}"
      when: ansible_facts.os_family == "Archlinux"
    - role: chezmoi
