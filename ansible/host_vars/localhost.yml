---
repo_dir: "{{ playbook_dir }}/.."
chezmoi_source_dir: "{{ repo_dir }}/chezmoi"

base_packages:
  - git
  - curl
  - zsh
  - chezmoi

# Optional additions enabled on this machine.
# A typical laptop could instead use: [laptop, media_apps]
workstation_features:
  - corsair
  - media_apps
